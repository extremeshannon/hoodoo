# Copy to .env and adjust. Used when running uvicorn locally from ./backend

DATABASE_URL=postgresql+psycopg://hoodoo:hoodoo@localhost:5432/hoodoo
# Optional; auto-detected if unset (repo root with data/catalog.json)
# REPO_ROOT=
